<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h2>Hey Raj,</h2>
    <h4><%= @user.name %> has joined Grassy!</h4> 
    <h4>Users email is <%= @user.email %> and the account was created on <%= @user.created_at.strftime("%A, %d %b %Y %l:%M %p") %></h4>
  </body>
</html>